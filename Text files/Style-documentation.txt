Mixins til:

Section one --> Font-size, font-family, placering (Flex), colors.

Icon Links --> Størrelser (imgSize), placering (flex/grid)

Event Info --> Størrelser (Font size, imgSize), colors, placering, font-family. 

Cards under Program --> Grid, størrelser(Font size, imgSize), colors, text-decoration, font-family. 

Nyhedsbokse --> Placering (Grid/Flex), colors, størrelser (Font-size, imgSize).




Parcials til: 
- Sections
- Tilmelding (Selve formen på Tilmeldings siden)
- Colors (Farve variabler som kan bruges i scss senere)
- Fonte (Importer fonte fra GoogleFonts)
- Ikon sektionen. (Sektionen med ikonerne)
- Nyhedsbokse (Med en mixin i)
- Nav + Footer. (Lav scss for både Nav og footer i samme parcial så der ikke bliver så meget kode i stylesheetet)